# Server Configuration
server.port=8080

# Database Configuration
spring.datasource.url=jdbc:postgresql://localhost:5432/gymsettracker
spring.datasource.username=postgres
spring.datasource.password=$tarko5
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# SQL initialization - run only if tables are empty
spring.sql.init.mode=never
spring.sql.init.data-locations=classpath:db/data.sql
spring.jpa.defer-datasource-initialization=true

# Only enable SQL initialization for the first run (change to "always" temporarily if needed)
# spring.sql.init.mode=always

# JWT Configuration
app.jwt.secret=c2VjdXJlLWp3dC1zZWNyZXQta2V5LWZvci1neW0tc2V0LXRyYWNrZXItYXBwbGljYXRpb24tMjAyMy1zdXBlci1zZWNyZXQK
app.jwt.expirationMs=86400000

# Logging
logging.level.com.treningi.app=DEBUG

# Server response compression
server.compression.enabled=true
server.compression.mime-types=application/json,application/xml,text/html,text/plain
